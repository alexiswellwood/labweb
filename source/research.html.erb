---
title: Research
date: 2015-08-10
author: Alexis Wellwood
pageable: true
per_page: 10
blog: research
---

<h2 class="pageheader">Current projects</h2>

<div class="row">
  <div class="col-md-6">
    <h4 style="color:red;">Measuring &amp; comparing</h4>
    <p> When is one thing <i>more</i> than another? What about <i>less</i>? How does language influence how a child decides, and how is that understanding similar or different from an adult's? We play fun games that involve comparing cartoon characters or simple objects to learn more about how children leverage their language and categorization abilities to perform simple comparisons.
    </p>
    <h5>Northwestern IRB #STU00201703</h5>
  </div>
  <div class="col-md-6">
    <h4 style="color:red;">Thinking about events</h4>
    <p>Cognitive scientists have discovered that children have a sophisticated ability to reason about the properties of objects like toys and substances like water. Much less is known about how they think about events like jumping or flashing, and processes like floating or glowing. We're learning more about this by investigating how children learn verbs!</p>
    <h5>Northwestern IRB #STU00201135</h5>
  </div>
</div>


<h2 class="pageheader">Publications</h2>

<% if paginate && num_pages > 1 %>
  <p>Page <%= page_number %> of <%= num_pages %></p>

  <% if prev_page %>
    <p><%= link_to 'Previous page', prev_page %></p>
  <% end %>
<% end %>

<% page_articles.each_with_index do |article, i| %>
  <h3>
    <span class="title"><%= article.data.title %> </span>
    <span class="bigsubheader">
      <span class="article-links">
        <% if article.data.local %>
          <span><%= link_to '<i class="fa fa-file-pdf-o"></i>'.html_safe, article.data.local %></span>
        <% end %>
        <% if article.data.official %>
          <span><%= link_to '<i class="fa fa-university"></i>'.html_safe, article.data.official %></span>
        <% end %>
        <%= article.date.strftime("%Y") %>
      </span>
  </h3>
  <h5>by <%= article.data.authors %>; <%= article.data.venue %></h5>
  <%= article.body %>
  <!-- requires Nokogiri: -->
  <!-- <%= article.summary(1000) %> <%= link_to 'Read more…', article %> --> 
  <% article.tags.each_with_index do |tag, j| %>
    <%= link_to tag, tag_path(tag) %> <%= '|' if j < (article.tags.size - 1) %>
  <% end %>
<% end %>

<!--gets articles by date
<ul>
  <% blog.articles.group_by {|a| a.date.year }.each do |year, articles| %>
    <li>
      <h5><%= year %></h5>
      <ul>
        <% articles.each do |article| %>
          <li><a href="<%= article.url %>" class="lowercase"><%= article.title %></a></li>
        <% end %>
      </ul>
    </li>
  <% end %>
</ul>
/gets articles by date-->

<!-- gets articles by tags
<ul>
  <% blog.tags.each do |tag, articles| %>
    <li>
      <h5><%= tag %></h5>
      <ul>
        <% articles.each do |article| %>
          <li><a href="<%= article.url %>" class="lowercase"><%= article.title %></a></li>
        <% end %>
      </ul>
  <% end %>
</ul>
/gets articles by tags-->

<% if paginate %>
  <% if next_page %>
    <p><%= link_to 'Next page', next_page %></p>
  <% end %>
<% end %>
